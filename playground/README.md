# Playground (minimal, no UI)

Este directorio contiene una app de Nuxt mínima usada para probar el módulo sin componentes UI.

- Ejecutar en modo desarrollo desde la raíz del proyecto:

  pnpm dev

- Endpoint de prueba:

  GET /api/health -> { ok: true, message: 'Playground API (no UI) is running' }
